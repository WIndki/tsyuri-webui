/**
 * useAbout Hook - 封装关于信息组件的业务逻辑
 * @returns {Object} 包含关于信息组件相关业务逻辑的对象
 */
export const useAbout = () => {
    // 目前About组件没有复杂的业务逻辑，主要是UI渲染
    // 但为了保持一致的架构模式，仍然提供这个Hook
    
    return {
        // 如果以后需要添加业务逻辑，可以在这里添加
    };
};
